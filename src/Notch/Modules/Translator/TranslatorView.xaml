<UserControl x:Class="Notch.Modules.Translator.TranslatorView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:local="clr-namespace:Notch.Modules.Translator">
 <UserControl.InputBindings><KeyBinding Key="Enter" Modifiers="Control" Command="{Binding TranslateCommand}" /></UserControl.InputBindings>
 <UserControl.Resources>
  <Style TargetType="ComboBox">
   <Setter Property="Foreground" Value="#EEEEF0"/><Setter Property="Background" Value="#19191B"/>
   <Setter Property="FontSize" Value="11"/><Setter Property="DisplayMemberPath" Value="Name"/>
   <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ComboBox">
    <Grid>
     <ToggleButton Focusable="False" IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
      <ToggleButton.Template><ControlTemplate TargetType="ToggleButton"><Border Background="#19191B" CornerRadius="7" Padding="8,5"><ContentPresenter/></Border></ControlTemplate></ToggleButton.Template>
      <StackPanel Orientation="Horizontal"><TextBlock Text="{Binding SelectionBoxItem.Name, RelativeSource={RelativeSource AncestorType=ComboBox}}" FontSize="11"/><TextBlock Text="⌄" Margin="8,0,0,0" FontSize="11" Foreground="#99999F"/></StackPanel>
     </ToggleButton>
     <Popup x:Name="PART_Popup" IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" AllowsTransparency="True" Focusable="False">
      <Border Background="#202023" BorderBrush="#37373B" BorderThickness="1" CornerRadius="8" Padding="4" MinWidth="125"><ScrollViewer MaxHeight="230"><ItemsPresenter/></ScrollViewer></Border>
     </Popup>
    </Grid>
   </ControlTemplate></Setter.Value></Setter>
  </Style>
  <Style TargetType="ComboBoxItem"><Setter Property="Foreground" Value="#EEEEF0"/><Setter Property="Padding" Value="8,5"/><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ComboBoxItem"><Border x:Name="Row" Background="Transparent" CornerRadius="5" Padding="{TemplateBinding Padding}"><ContentPresenter/></Border><ControlTemplate.Triggers><Trigger Property="IsHighlighted" Value="True"><Setter TargetName="Row" Property="Background" Value="#3A3A3E"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter></Style>
  <Style TargetType="TextBox"><Setter Property="Foreground" Value="#EEEEF0"/><Setter Property="CaretBrush" Value="White"/><Setter Property="Background" Value="Transparent"/><Setter Property="BorderThickness" Value="0"/><Setter Property="TextWrapping" Value="Wrap"/><Setter Property="AcceptsReturn" Value="True"/><Setter Property="VerticalScrollBarVisibility" Value="Auto"/><Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/><Setter Property="FontSize" Value="12"/><Setter Property="Padding" Value="8,5"/></Style>
 </UserControl.Resources>
 <Grid>
  <Grid.RowDefinitions><RowDefinition Height="28"/><RowDefinition Height="*"/><RowDefinition Height="*"/><RowDefinition Height="26"/></Grid.RowDefinitions>
  <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="30"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
   <ComboBox ItemsSource="{x:Static local:TranslatorViewModel.SourceLanguages}" SelectedItem="{Binding Source}" AutomationProperties.Name="Исходный язык"/>
   <Button Grid.Column="1" Content="⇄" Style="{StaticResource NotchButton}" Padding="0" Command="{Binding SwapCommand}" ToolTip="Поменять языки"/>
   <ComboBox Grid.Column="2" ItemsSource="{x:Static local:TranslatorViewModel.TargetLanguages}" SelectedItem="{Binding Target}" AutomationProperties.Name="Язык перевода"/>
  </Grid>
  <Border Grid.Row="1" Background="#151517" CornerRadius="9" Margin="0,6,0,3">
   <Grid><TextBox Text="{Binding Input, UpdateSourceTrigger=PropertyChanged}" MaxLength="5000" AutomationProperties.Name="Текст для перевода"/>
    <TextBlock Text="Введите или вставьте текст…" Margin="10,7" FontSize="12" Foreground="#77777F" IsHitTestVisible="False"><TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding Input}" Value=""><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></TextBlock.Style></TextBlock>
   </Grid>
  </Border>
  <Border Grid.Row="2" Background="#151517" CornerRadius="9" Margin="0,3,0,6"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="28"/></Grid.ColumnDefinitions><TextBox Text="{Binding Result, Mode=OneWay}" IsReadOnly="True" AutomationProperties.Name="Результат перевода"/><Button Grid.Column="1" VerticalAlignment="Top" Margin="0,3,3,0" Content="&#xE8C8;" Style="{StaticResource IconButton}" Command="{Binding CopyCommand}" ToolTip="Скопировать перевод"/></Grid></Border>
  <Grid Grid.Row="3"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="{Binding Status}" ToolTip="{Binding Status}" FontSize="9" Foreground="#99999F" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="0,0,5,0"/><Button Grid.Column="1" Content="Перевести" FontSize="11" Padding="10,3" Background="#242427" Style="{StaticResource NotchButton}" Command="{Binding TranslateCommand}" ToolTip="Ctrl+Enter · До 500 байт текста за перевод. MyMemory: 5 000 символов в сутки без ключа."/></Grid>
 </Grid>
</UserControl>

